repositoryID = test-repo
resourceCacheSize = 200
maxDatabaseConnections = 1
databaseDriver = org.hsqldb.jdbc.JDBCDriver
sqlDialect = hsqldb
databaseURL = jdbc:hsqldb:hsql://localhost/vtk-test
jdbcUsername = sa
jdbcPassword =
indexStorageRootPath = ${java.io.tmpdir}
repositoryDataDirectory = ${java.io.tmpdir}
repositoryTrashCanDirectory = ${java.io.tmpdir}
repositoryRevisionDirectory = ${java.io.tmpdir}
# Ordered list of authentication handlers (bean refs) to use:
authenticationHandlers =
